desktop v0.2.86 — 微信式细滚动条(悬停/滚动时才显示);agent-node 节点即使未上报能力也显示规则文件区块
================================================================================

来源:Vincent 2026-09-24 对 0.2.85 的反馈;发版批准「行」。

A. 滚动条(#335)
--------------------------------------------------------------------------------
  列表与会话滚动条改细、默认隐藏,悬停或滚动时出现(参照微信)。
  ⚠ 只在 Chromium(Playwright + expo web 导出)上验证过;macOS 桌面端用的是 WebKit,
    滚动条外观需要在真机构建上看一眼再确认。

B. 规则文件区块(#336)
--------------------------------------------------------------------------------
  agent-node 类节点(opencode / codex 等)即使还没升级到上报 rules_file_capable 的版本,也显示规则文件区块;
  agent-node:claude 类节点标注为 CLAUDE.md,其余为 AGENTS.md。Claude Code 会话节点仍按能力标志显示。

C. 验证边界(如实)
--------------------------------------------------------------------------------
  本机 Linux 跑不了 Tauri 壳;界面验证为 web 导出 + Playwright。Mac / Windows 真机外观以发版后实际截图为准。
