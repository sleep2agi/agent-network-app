desktop v0.2.94 — 节点设置新增「项目文件夹」
================================================================================

来源:Vincent 2026-09-25 提出「设置里面来一个 项目文件夹？」。协议与安全设计见 sleep2agi/agent-network#1999。

A. 改动(#357)
--------------------------------------------------------------------------------
  节点页在「技能」与「任务」之间新增「项目文件夹」区,沿用 #355 的一行工具栏(面包屑 · 条目数 · ⓘ · 刷新)。
  目录逐层进入、「上一级」返回;文件点开查看:Markdown 走 MarkdownMessage 渲染,代码带行号(不折行、横向滚动),
  其他按纯文本显示。凭据文件(.env、密钥等)只列文件名,App 不对它们发任何读取请求;过大/二进制文件只显示大小;
  node_modules、.git、指向工作目录外的链接不跟进。列表被截断时显示「只显示前 N 项(共 M 项)」。
  发请求前先看节点上报的 files_capable:没有就立即提示要升级什么(agent-node ≥ 2.5.0-preview.88 / anet ≥
  2.3.0-preview.115 / commhub-server ≥ 0.9.0-preview.59),不会 60 秒空转;版本够但尚未上报的节点提示重启。
  0.2.93 版本号提交之后 main 上只合入了 #357 这一个 PR。

B. 验证边界(如实)
--------------------------------------------------------------------------------
  #357 自测:src/node-files.test.ts 57/57;npm test 125/125 文件;tsc -p tsconfig.build.json 0 错误。
  Playwright(web 导出 + 模拟 __TAURI_INTERNALS__,一次性 hub + 真实 agent-node):浅色/深色截图覆盖
  目录根、子目录、Markdown、代码、被拒绝的 .env、旧节点升级提示;页面错误 0。
  本 PR 只改版本号与发版说明;version-consistency 测试通过。Mac / Windows 真机外观以发版后实际截图为准。
