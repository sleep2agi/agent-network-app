desktop v0.2.101 — 手机上编辑规则文件 + 主题「跟随系统」
================================================================================

A. 本版内容(0.2.100 版本号提交 #377 之后 main 上合入的 PR + 本 PR)
--------------------------------------------------------------------------------
  #378 规则文件在手机上可编辑:规则分区不再放在页面外层 ScrollView 里,工具栏(阅读/编辑、文件名、保存)
       固定不被滚走;阅读区 nestedScrollEnabled;窄屏(<560)工具栏两行;原生端双击某个块跳到源码对应行
       (TextInput.setSelection,键盘收缩页面后 350ms 再设一次);原生规则页启用 KeyboardAvoidingView,
       键盘弹起时收起头部卡片;有未保存修改时切分区 / 头部返回 / 安卓返回键先确认「放弃未保存的修改?」。
       npm test 137/137,变异 23/23 全部抓到。
  #379 主题「跟随系统」:偏好 light / dark / system 与生效主题分离;没有存储值(新安装)默认跟随系统,
       已存的 light / dark 原样保留;桌面 / web 用 matchMedia,安卓 / iOS 用 Appearance 实时跟随;
       app.json userInterfaceStyle dark → automatic;设置里三段选择 浅色 / 深色 / 跟随系统。
       theme-follow-system.test.ts 59 条,变异 22 个 21 红(存活的 1 个等价)。
  本 PR(版本号)
       版本号 8 个文件 → 0.2.101,android.versionCode 2100 → 2101;Cargo.lock 只改根包一段。
       release-desktop-auto-update.yml 加 What's new in 0.2.101。

B. 验证边界(如实)
--------------------------------------------------------------------------------
  本 PR 只改版本号与说明;功能的验证见 #378 / #379 各自的 PR(web 导出 + Playwright,占位数据)。
  #378 的原生双击跳转、键盘让位,以及 #379 在安卓上随系统切换深浅色,只能在真机上最终确认。
