desktop v0.2.83 — 设置页改成「左栏分类 + 右栏行式选项 + 搜索」;通知点击跳会话去掉错误前置
================================================================================

来源：Vincent 2026-09-20 task 6562cc89「设置改为这种样式和交互吧」(截图：Claude 桌面端设置弹层)
      Vincent 2026-09-20 task 8a0afd60「消息点击通知 还是不通跳转到对于的聊天界面」(截图：Windows toast)

A. 设置页
--------------------------------------------------------------------------------
参照物：左上关闭按钮;左栏 = 搜索框 + 分类列表(图标 + 名字,当前项高亮);右栏 = 分类标题 +
       「标签在左、控件在右」的行(下拉值带 chevron / 开关),需要说明的行下面一句灰字;段间细线。

映射(只映射真实存在的设置,一项没加、一项没丢):
  账号     账号与 Hub(profile 列表 / 新窗口 / 移除)、添加 Hub / 账号、移除当前账号
  本地 Hub 状态、地址、Hub 版本、升级(条件)、重新启动、停止、打开日志、立即备份、
           删除本地工作区数据 → 单独红边「危险区」
  外观     主题(下拉样式值 + chevron)
  通知     消息提示音(Switch)、免打扰时段(Switch + 起止时间)、说明
  关于     版本、软件更新

搜索：按行标签 + 关键词跨分类筛(`settings-model.ts` 纯模型);当前分类没命中时切到第一个有命中的;
      搜索中右栏标题「搜索结果」并按分类分组显示命中行。
窄屏(<640)：左栏变顶部横向分类条。桌面端传 onClose 才画关闭按钮(手机端设置是 tab)。

保留：0.2.80 的滚动修复(右栏 ScrollView,padding 在 contentContainer;两个确认 Modal 仍是
      ScrollView 的兄弟)——settings-scroll.test.ts 原样通过。

测试
  src/settings-model.test.ts   32/32(结构 / 搜索 / 可见行 / 分类切换 / 9 条 SettingsScreen 契约)
  见证：重写前在旧 SettingsScreen 上跑 → 契约 9 条红(23/32),重写后 32/32。
  src/settings-scroll.test.ts  原样通过。

真机验证(Playwright 桌面壳,mock __TAURI_INTERNALS__,一次性 hub 9299)
  深浅两主题各:账号 / 外观 / 通知 / 关于 / 搜索「版本」/ 520px 矮窗口。
  搜「版本」右栏 = 「搜索结果 | 关于 | 版本 | v0.2.82」(跨分类命中)。
  截图：scratchpad/v0283-settings-{light,dark}-{account,appearance,notifications,about,search,short}.png
  ⚠️ 这是渲染图不是真机截图;本机 Linux 跑不了 Tauri 壳。

B. 通知点击跳会话
--------------------------------------------------------------------------------
诊断(先诊后改)：0.2.81 的路由有四条前置——唯一 agent 待跳 / 20 s 内 / 通知发出那一刻窗口
**没有**焦点 / 一次性。逐条对 Vincent 的场景(正开着 A 的会话,B 来消息,点 B 的 toast):
  · 唯一 agent          → 不拦
  · 20 s 内             → 一般不拦(但偏紧)
  · 🔴 通知时窗口没焦点 → **拦住**:人正在应用里,窗口有焦点,`recordNotified` 直接返回原状态,
                           通知根本没被记下,随后的激活什么都不跳。
  · 一次性              → 不拦
见证：改前在旧 notify-target 上跑该场景(`recordNotified(...,'B',T0,true)` → `targetOnFocus(...,'A')`)
      → 路由到 null。

改法：
  · 通知一律记(去掉焦点前置);激活时(focus / visibilitychange→visible)才判——已在看目标会话
    则不跳。
  · 窗口期 20 s → 60 s:人常先把手上一句话打完再点,Windows 的 toast 收进通知中心后还能点。
  · 多个 agent 先后通知 → 跳最近一个(0.2.81 是不猜不跳)。理由:焦点推断本来分不出点的是哪张
    toast;300 台机群里两人同时说话是常态,「不跳」让功能在最常见情形下失效;跳错的代价是点一下
    回去。
  · 契约禁止 onAction(插件桌面端永远不触发)和「以当刻窗口没焦点为条件」回流。

测试  src/notify-target.test.ts 23/23(第一组即 Vincent 场景)。

⚠️ 本机验不了的：Windows 上点 toast 时,若应用已在前台,webview 是否会收到 blur→focus 对
   (或 visibilitychange)。若两者都不发生,这条路径在「应用已前台」时仍观测不到激活——
   这是插件不传点击事件的硬限制,不是本版能绕过的。装上后若仍不跳,需要 Windows 真机日志。

版本戳(9 处):app.json / package.json / package-lock.json×2 / src-tauri/Cargo.toml / Cargo.lock /
             tauri.conf.json / src/version.ts / src/version-consistency.test.ts
