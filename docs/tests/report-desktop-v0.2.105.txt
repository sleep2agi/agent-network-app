desktop v0.2.105 — 聊天界面精简:去掉 ⚡ 和 BTW(旁路提问)入口
================================================================================

A. 本版内容(0.2.104 版本号提交 #388 之后 main 上合入的 PR + 本 PR)
--------------------------------------------------------------------------------
  #390 聊天界面隐藏两个不常用入口(全平台:手机、安卓双栏、桌面、web):
       ⚡ = 「优先发送」一次性开关(下一条消息以 priority=high 发出,#139);
       BTW = 顶栏 BTW 按钮 + 「+」面板/桌面「+」弹层的「旁路提问」格子。
       只隐藏按钮,开关在 src/chat-entry-flags.ts(SHOW_BTW_ENTRY / SHOW_BOLT_ENTRY = false);
       输入框里打 `/btw 问题` 仍可打开旁路线程;优先级发送链路未改,现在一律按 normal 发。
  本 PR(版本号)
       版本号 8 个文件 → 0.2.105,android.versionCode 2104 → 2105;Cargo.lock 只改根包一段。
       release-desktop-auto-update.yml 加 What's new in 0.2.105。

B. 验证边界(如实)
--------------------------------------------------------------------------------
  本 PR 只改版本号与说明;功能验证见 #390(npm test / typecheck + web 导出 Playwright
  1200x850 安卓 UA 双栏、390x844 手机、1200x850 桌面,占位数据)。
  发布后镜像 dispatch + 匿名下载 sha 比对是安卓用户看到更新的前提(#385 读镜像 VERSION)。
