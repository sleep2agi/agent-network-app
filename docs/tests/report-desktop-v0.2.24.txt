Agent Network Desktop v0.2.24 release candidate

Scope
- Multi-Hub profile persistence, recovery, and per-profile reauthentication.
- Detached chat-window persistence across main-window restarts.
- Authenticated Tauri image thumbnails with retry and safe Blob URL cleanup.
- Synchronize every user-visible and package version to 0.2.24.

Release gates
- Version consistency test: passed (all eight version surfaces report 0.2.24).
- Docker npm ci / typecheck / full unit tests / Expo export: passed.
- Docker unit suite: 29/29 test files passed.
- Docker Rust cargo check: pending.
- GitHub Actions macOS arm64 + Windows x64 signed build: pending.
- Real install/runtime smoke: pending (Mac runner currently returns 402 quota exhausted).

Release policy
- The signed GitHub release remains a draft until install/runtime smoke passes.
- Issues #54 and #66 remain open until their explicit real-device matrices pass.
