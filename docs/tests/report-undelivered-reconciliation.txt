P0 — false "Not delivered / retry" reconciliation (#170)

Source commit under test: pending

Behavior changed
- A pending outbox row stays pending across process restart. It is unknown until Hub reconciliation, not a proven failure.
- A timeout refreshes the authoritative task list before exposing a red retry action. If the Hub row exists, the optimistic row is retired.
- One optimistic bubble owns one dreq correlation id. Repeated retries reuse it, including deterministic migration for legacy local-* rows.

Docker verification
- Image: anet-app-undelivered170:dev
- Full suite: 47/47 test files passed
- TypeScript: clean
- Focused request-id contract: 10/10
- Outbox lifecycle/process restart: 11/11
- ACK-loss reconciliation order: 3/3

Witnessed red
- Restore pending rows as failed again: outbox lifecycle falls to 8/11; both restart assertions fail.
- Inspect the optimistic row before Hub reconciliation: `Hub reconciliation retires an ACK-lost optimistic row before failure UI` fails.
- Ignore the caller's stable request id and generate one per HTTP call: `sendTask honors the bubble correlation id across retries` fails.

Boundary
- A genuine failed write still exposes one retry action after an authoritative refresh cannot find it.
- Historical red rows created by older app versions remain user-actionable; this patch prevents new ambiguous rows from being misclassified and reconciles matching Hub rows.
