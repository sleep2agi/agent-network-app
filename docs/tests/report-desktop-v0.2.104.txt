desktop v0.2.104 — 安卓更新走国内镜像 + 微信式「+」面板 + 全屏规则编辑让开状态栏
================================================================================

A. 本版内容(0.2.103 版本号提交 #384 之后 main 上合入的 PR + 本 PR)
--------------------------------------------------------------------------------
  #385 安卓检查更新先读 ModelScope 镜像 desktop/latest/VERSION,GitHub 兜底,不再被 GitHub 限流;
       下载的 APK 装前校验 sha256(新增 expo-crypto 依赖)。
  #386 聊天输入框「+」改成微信式一级面板:相册、文件、旁路提问、拍照一步直达
       (app.json 加 iOS cameraPermission,拍照用 expo-image-picker;安卓 CAMERA 权限来自库清单)。
  #387 安卓全屏编辑规则文件时,顶部按钮让开状态栏与挖孔区域。
       说明:规则编辑器行高不均的问题本版没有修复。
  本 PR(版本号)
       版本号 8 个文件 → 0.2.104,android.versionCode 2103 → 2104;Cargo.lock 只改根包一段。
       release-desktop-auto-update.yml 加 What's new in 0.2.104。

B. 验证边界(如实)
--------------------------------------------------------------------------------
  本 PR 只改版本号与说明;功能验证见 #385 / #386 / #387 各自的 PR。
  发布后镜像 dispatch + 匿名下载 sha 比对是安卓用户看到更新的前提(#385 读镜像 VERSION)。
