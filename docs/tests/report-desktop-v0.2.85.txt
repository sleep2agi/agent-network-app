desktop v0.2.85 — 极简风 UI 重做;规则文件区块按节点能力显示;模型候选去掉下架模型;grok TUI 标预览
================================================================================

来源：Vincent 2026-09-24「桌面端的 UI/UX 设计你看着弄成极简风，用你的审美升级一下」「全力推进 UI/UX 的体验」;
      「设置里面也没读到 AGENTS.md 啊」(截图:Claude Code 会话节点的「节点信息」页);审阅对比图后「行」。

A. 极简风重做(#333)
--------------------------------------------------------------------------------
  设计 token 统一到 src/theme.ts(调色板深/浅、半径 6/10/14/pill、字号 11/12/14/16/20、字重 400/500/600 封顶);
  层次靠底色区分不靠边框;强调色只留在侧栏、主按钮、链接;深色强调色由 #22d3ee 改为 #4cc3d6;
  53 处 700/800 字重降到 600;聊天气泡去边框、引用改左侧 2px 细线;会话列表扁平化;任务筛选改分段标签;
  服务器/新建节点/设置卡片去边框、选中态中性色;窗口底色(tauri.conf.json)同步新深色底。
  对比度:所有文字/底色对 ≥ 4.5:1(最弱 4.57 浅色 / 4.69 深色),全表见 docs/design/minimal-2026-09.md。
  测试:新增 src/theme-minimal.test.ts 51 条(新 token + 旧界面时 4 条红);theme-light / window-background /
       desktop-navigation 三个钉旧值的测试改为断言 token。
  未改:托盘面板(独立 Tauri 窗口,web 导出渲染不到)。

B. 规则文件区块(#334,配套主仓 #1977)
--------------------------------------------------------------------------------
  节点上报 rules_file_capable 即显示,含只读「节点信息」页;有节点行按 node_id,否则按别名定位会话。
  需要 hub commhub-server 0.9.0-preview.56 + 节点 agent-network 2.3.0-preview.111 / agent-node 2.5.0-preview.84;
  旧节点不上报时行为与 0.2.84 相同(区块隐藏)。

C. 其它
--------------------------------------------------------------------------------
  #330 模型候选去掉已下架的 opencode/mimo-v2.5-free,当前模型不在候选中仍显示为选中。
  #331 iOS Info.plist 声明豁免加密(ITSAppUsesNonExemptEncryption=false)+ TestFlight 合规专用模式。
  #332 创建向导里 Grok TUI 共存标「预览」,并提示稳定的 Grok(build-acp)。
  #329 发版工作流 0.2.84 更新说明块(记录用)。

D. 验证边界(如实)
--------------------------------------------------------------------------------
  界面渲染是 expo web 导出 + Playwright(模拟 Tauri 壳)在一次性 hub 上截的图,深浅各 10 屏;
  本机 Linux 跑不了 Tauri 壳,Mac / Windows 真机外观以发版后用户实际截图为准。
