desktop v0.2.87 — 节点页重做 + 只读技能区块 + 长串不再撑破气泡 + 切主题停留在外观
================================================================================

来源:Vincent 2026-09-24 对节点页、聊天、设置的反馈;发版批准「发」。

A. 节点页重做(#338)
--------------------------------------------------------------------------------
  头部卡片(名称/状态/运行时)+ 分区导航(概览 / 模型与运行时 / 规则文件 / 技能 / 任务 / 危险操作);
  节点发给自己的提醒类任务单独分组,不计入「运行中」。

B. 只读技能区块(#339)
--------------------------------------------------------------------------------
  列出节点已有的技能,只读。需要 hub 与节点都支持上报技能后才显示;在那之前区块隐藏(与旧行为相同)。

C. 聊天长串(#337)
--------------------------------------------------------------------------------
  行内代码、哈希、URL 等长串在气泡内换行,不再溢出。

D. 设置(#340)
--------------------------------------------------------------------------------
  在「外观」里切换主题后停留在「外观」,不再跳回「账号」。

E. 验证边界(如实)
--------------------------------------------------------------------------------
  本机 Linux 跑不了 Tauri 壳;界面验证为 web 导出 + Playwright。Mac / Windows 真机外观以发版后实际截图为准。
